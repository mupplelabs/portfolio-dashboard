# Data Directory

This directory is used to store persistent data such as SQLite databases.
It is intentionally tracked in the repository to ensure the directory structure exists when building the Docker container or cloning the repository.
